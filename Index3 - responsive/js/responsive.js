$(document).ready(function() {

  let heightSlide = window.innerHeight;
  let slideStyle = $('#cel')[0].style;

    slideStyle.height = heightSlide + 'px';

    $(window).resize(function(){
      heightSlide = window.innerHeight;
      slideStyle.height = heightSlide + 'px';

});

});

$(document).ready(function() {

  let heightSlide = window.innerHeight;
  let slideStyle = $('#mulherfundo')[0].style;

    slideStyle.height = heightSlide + 'px';

    $(window).resize(function(){
      heightSlide = window.innerHeight;
      slideStyle.height = heightSlide + 'px';

});

});

$(document).ready(function() {

  let heightSlide = window.innerHeight;
  let slideStyle = $('#slide2')[0].style;

    slideStyle.height = heightSlide + 'px';

    $(window).resize(function(){
      heightSlide = window.innerHeight;
      slideStyle.height = heightSlide + 'px';

});

});
$(document).ready(function() {

  let heightSlide = window.innerHeight;
  let slideStyle = $('#slide3')[0].style;

    slideStyle.height = heightSlide + 'px';

    $(window).resize(function(){
      heightSlide = window.innerHeight;
      slideStyle.height = heightSlide + 'px';

});

});

$(document).ready(function() {

  let heightSlide = window.innerHeight;
  let slideStyle = $('#slide4')[0].style;

    slideStyle.height = heightSlide + 'px';

    $(window).resize(function(){
      heightSlide = window.innerHeight;
      slideStyle.height = heightSlide + 'px';

});

});
$(document).ready(function() {

  let heightSlide = window.innerHeight;
  let slideStyle = $('#slide5')[0].style;

    slideStyle.height = heightSlide + 'px';

    $(window).resize(function(){
      heightSlide = window.innerHeight;
      slideStyle.height = heightSlide + 'px';

});

});
$(document).ready(function() {

  let heightSlide = window.innerHeight;
  let slideStyle = $('#video2')[0].style;

    slideStyle.height = heightSlide + 'px';

    $(window).resize(function(){
      heightSlide = window.innerHeight;
      slideStyle.height = heightSlide + 'px';

});

});
$(document).ready(function() {

  let heightSlide = window.innerHeight;
  let slideStyle = $('#slide6')[0].style;

    slideStyle.height = heightSlide + 'px';

    $(window).resize(function(){
      heightSlide = window.innerHeight;
      slideStyle.height = heightSlide + 'px';

});

});

$(document).ready(function() {

  let heightSlide = window.innerHeight;
  let slideStyle = $('#slide7')[0].style;

    slideStyle.height = heightSlide + 'px';

    $(window).resize(function(){
      heightSlide = window.innerHeight;
      slideStyle.height = heightSlide + 'px';

});

});

$(document).ready(function() {

  let heightSlide = window.innerHeight;
  let slideStyle = $('#slide8')[0].style;

    slideStyle.height = heightSlide + 'px';

    $(window).resize(function(){
      heightSlide = window.innerHeight;
      slideStyle.height = heightSlide + 'px';

});

});

$(document).ready(function() {

  let heightSlide = window.innerHeight;
  let slideStyle = $('#slide9')[0].style;

    slideStyle.height = heightSlide + 'px';

    $(window).resize(function(){
      heightSlide = window.innerHeight;
      slideStyle.height = heightSlide + 'px';

});

});
